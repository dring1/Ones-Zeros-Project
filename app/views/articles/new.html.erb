<h1>Submit an Article!</h1>

<%= form_for @article do |f| %>
	<%= f.label "URL Name: " %>
	<%= f.text_field :name %>

	<%= f.label "URL : " %>
	<%= f.text_field :url %>	

	<%= f.label "Topics? (delimeter by , )" %>
	<%= f.text_field :topics %>

	<%= f.label "Preview title: " %>
	<%=  %>
 
	<%= f.submit "Submit URL" %>
<% end %>